# Repository blog posts

Eve opens draft pull requests that add one validated JSON file to this directory.
Merging the pull request publishes the post on the next deployment.

Do not add secrets or unpublished operational data. Every JSON file is validated
during the application build; an invalid post fails the build instead of being
silently omitted.
